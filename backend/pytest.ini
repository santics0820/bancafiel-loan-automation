# Run from the backend/ directory: cd backend && pytest
# Coverage example: pytest --cov=src/lambdas/fraud-detector --cov-report=term-missing
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
